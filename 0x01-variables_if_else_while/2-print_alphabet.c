#include <stdio.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	char ch;
	int nl = 10;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);

	putchar(nl);

	return (0);
}
