#include "holberton.h"
/**
 * mul - multiplies two integers.
 * @a: multiplicand.
 * @b: multiplier.
 * Return: the product of the multiplication.
 */

int mul(int a, int b)
{
	int product;

	product = (a * b);

	return (product);
}
